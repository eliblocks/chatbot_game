<h4>Games</h4>
<div>
  <%= link_to "New Game", new_game_path %>
  <%= button_to "Create Game", games_path, class: "mt-2" %>
</div>
<ul class="mt-4">
  <% Game.all.each do |game| %>
    <li>
      <%= link_to game.id, game_path(game)%>
    </li>
  <% end %>
</ul>
